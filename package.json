{
  "name": "metro-magnate-server",
  "version": "1.0.0",
  "description": "Metro Magnate WebSocket Game Server",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "vercel-build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
